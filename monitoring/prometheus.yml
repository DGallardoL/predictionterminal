global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: pfm-api

scrape_configs:
  - job_name: 'pfm-api'
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']
        labels:
          env: dev
